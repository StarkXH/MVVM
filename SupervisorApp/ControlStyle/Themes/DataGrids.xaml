<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- 🎨 DataGrid 色彩方案 -->
    <SolidColorBrush x:Key="DataGrid.Primary" Color="#005CB9"/>
    <SolidColorBrush x:Key="DataGrid.Background" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="DataGrid.AlternateRow" Color="#F8F9FA"/>
    <SolidColorBrush x:Key="DataGrid.Border" Color="#E9ECEF"/>
    <SolidColorBrush x:Key="DataGrid.Header.Background" Color="#F1F3F4"/>
    <SolidColorBrush x:Key="DataGrid.Header.Foreground" Color="#212529"/>
    <SolidColorBrush x:Key="DataGrid.Selected" Color="#E3F2FD"/>
    <SolidColorBrush x:Key="DataGrid.Selected.Border" Color="#2196F3"/>
    <SolidColorBrush x:Key="DataGrid.Hover" Color="#F5F5F5"/>
    <SolidColorBrush x:Key="DataGrid.Focus" Color="#1976D2"/>

    <!-- 🔹 简约高级DataGrid样式 -->
    <Style x:Key="SupervisorDataGridStyle" TargetType="{x:Type DataGrid}">
        <Setter Property="Background" Value="{StaticResource DataGrid.Background}"/>
        <Setter Property="Foreground" Value="#212529"/>
        <Setter Property="BorderBrush" Value="{StaticResource DataGrid.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource DataGrid.Border}"/>
        <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource DataGrid.AlternateRow}"/>
        <Setter Property="RowHeight" Value="25"/>
        <Setter Property="ColumnHeaderHeight" Value="40"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="SelectionMode" Value="Single"/>
    </Style>

    <!-- 🔹 列标题样式 -->
    <Style x:Key="SupervisorDataGridColumnHeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
        <Setter Property="Background" Value="{StaticResource DataGrid.Header.Background}"/>
        <Setter Property="Foreground" Value="{StaticResource DataGrid.Header.Foreground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource DataGrid.Border}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#E8F4FD"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#BBDEFB"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- 🔹 行样式 -->
    <Style x:Key="SupervisorDataGridRowStyle" TargetType="{x:Type DataGridRow}">
        <Setter Property="Background" Value="Transparent"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource DataGrid.Hover}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource DataGrid.Selected}"/>
                <Setter Property="BorderBrush" Value="{StaticResource DataGrid.Selected.Border}"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- 🔹 单元格样式 -->
    <Style x:Key="SupervisorDataGridCellStyle" TargetType="{x:Type DataGridCell}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Foreground" Value="#212529"/>
                <Setter Property="Background" Value="Transparent"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource DataGrid.Focus}"/>
                <Setter Property="BorderThickness" Value="2"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>